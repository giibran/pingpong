<h1>Editing match</h1>

<%= render 'form' %>

<%= link_to 'Show', @match %> |
<%= link_to 'Back', matches_path %> |||
<%= link_to 'Destroy', @match, method: :delete, data: { confirm: 'Are you sure?' } %>
